#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;

int 
int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    int T, N, M, K;
    scanf("%d", &T);
    for(int i=0;i<T;++i)
    {
        scanf("%d%d%d", &N, &M, &K);
    }
    return 0;
}
